Summary

- Migration to Vite and compatibility fixes.
- Performance improvement: cache Intl.NumberFormat in src/lib/format.ts and reuse via `formatCurrency` in components to avoid creating formatters per render.

Build & tests

- `pnpm run build` completed successfully (bundle output in `dist/`).
- Vitest: 4 files, 19 tests passed.

Files changed (high level)

- src/lib/format.ts
- src/components/checkout/CheckoutModal.tsx
- src/components/checkout/PixPayment.tsx
- src/components/VariacoesModal.tsx

References

- Used closed PRs #5, #6, and #7 as references for migration steps.

Next steps

- Manual QA on checkout/payment flows.
- Re-run Vercel preview (authorization may be required) and inspect deploy logs if it still fails.


(Updated by automation to fix PR body formatting.)
